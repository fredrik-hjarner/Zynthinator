export const getScrollbarWidth = element => (element ? element.offsetWidth - element.clientWidth : 0);
export const getScrollbarHeight = element => (element ? element.offsetHeight - element.clientHeight : 0);
