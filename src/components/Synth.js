import React from 'react';
import {
  // AllTriggers,
  ModalWindow,
  MessageModal,
  NodeGraph
} from '../components';

export const Synth = () => (
  <div>
    <NodeGraph />
    <ModalWindow />
    <MessageModal />
  </div>
);
